font-height: 16

font-files:
 - /usr/share/fonts/truetype/unifont/unifont.ttf
 - /usr/share/wine/fonts/wingding.ttf

local-output: false

fork: false

width: 80
height: 25

telnet-addr: 127.0.0.1
# set to 0 to disable
telnet-port: 2300

http-port: 8080

ssh-addr: 127.0.0.1
# set to 0 to disable
ssh-port: 2200
#ssh-keys: /etc/ssh
ssh-keys: keys/

#exec-command: /usr/bin/irssi -c oftc
exec-command: /usr/bin/htop
directory: /tmp

logging:
  file: termcamng.log
  loglevel-files: debug
  loglevel-screen: debug
